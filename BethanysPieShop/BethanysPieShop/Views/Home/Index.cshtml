@model  HomeViewModel

@await Html.PartialAsync("carousal")

<h2>Pies of the week</h2>
<h4>Our weekly selection - just for you!</h4>

<div class="row">
    @foreach(var pie in Model.PiesOfTheWeek)
    {
        @await Html.PartialAsync("PieOfTheWeekSummary", pie);
    }
</div>